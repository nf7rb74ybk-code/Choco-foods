{
  "name": "choco-ship-app",
  "version": "1.0.0",
  "private": true,
  "description": "CHOCO SHIP customer, shipper and admin app",
  "scripts": {
    "sync": "npx cap sync",
    "copy": "npx cap copy",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/ios": "^7.4.3",
    "@capacitor/android": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
