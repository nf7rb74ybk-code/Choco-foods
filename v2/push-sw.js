self.addEventListener('push',event=>{let d={};try{d=event.data?event.data.json():{}}catch(_){d={title:'CHOCO SHIP',body:event.data?event.data.text():'Có thông báo mới'}}const title=d.title||'CHOCO SHIP';const options={body:d.body||'Có cập nhật mới',icon:'../icon-512x512.png',badge:'../icon-512x512.png',data:{url:d.url||'./index.html'},tag:d.code?`choco-order-${d.code}`:'choco-push',renotify:true};event.waitUntil(self.registration.showNotification(title,options))});self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'./index.html';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus'in c){c.focus();return c.navigate(url)}}return clients.openWindow(url)}))});