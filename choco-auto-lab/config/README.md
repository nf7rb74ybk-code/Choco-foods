# CHOCO AUTO LAB — Configuration

This directory is reserved for non-production configuration and documentation.

Rules:
- Never store production secrets, service-role keys, VAPID private keys, or OneSignal secrets here.
- Use environment variables/secrets for future LAB execution.
- Initial AUTO implementation must remain read-only.
- Any future write capability requires an explicit safety review and Admin approval flow.
