name: Build CHOCO SHIP iOS

on:
  workflow_dispatch:
  push:
    paths:
      - 'app-build/**'
      - '.github/workflows/build-ios.yml'

permissions:
  contents: read

jobs:
  ios-simulator:
    runs-on: macos-15
    defaults:
      run:
        working-directory: app-build
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: app-build/package.json

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Add Capacitor iOS
        run: npx cap add ios

      - name: Sync Capacitor iOS
        run: npx cap sync ios

      - name: Verify iOS project
        run: test -f ios/App/App.xcodeproj/project.pbxproj

      - name: Build iOS Simulator
        run: |
          xcodebuild \
            -workspace ios/App/App.xcworkspace \
            -scheme App \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath ios/build \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Verify iOS app
        run: test -d ios/build/Build/Products/Debug-iphonesimulator/App.app

      - name: Package Simulator app
        run: ditto -c -k --sequesterRsrc --keepParent ios/build/Build/Products/Debug-iphonesimulator/App.app choco-ship-ios-simulator.zip

      - name: Upload iOS Simulator artifact
        uses: actions/upload-artifact@v4
        with:
          name: choco-ship-ios-simulator
          path: app-build/choco-ship-ios-simulator.zip
          if-no-files-found: error

  ios-device-prepare:
    runs-on: macos-15
    defaults:
      run:
        working-directory: app-build
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: app-build/package.json

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Add Capacitor iOS
        run: npx cap add ios

      - name: Sync Capacitor iOS
        run: npx cap sync ios

      - name: Verify device project
        run: test -f ios/App/App.xcodeproj/project.pbxproj

      - name: Verify iOS device archive prerequisites
        run: |
          xcodebuild -version
          xcodebuild -list -workspace ios/App/App.xcworkspace

      - name: Package iOS project for signing on Mac
        run: |
          ditto -c -k --sequesterRsrc --keepParent ios choco-ship-ios-project.zip

      - name: Upload iOS project
        uses: actions/upload-artifact@v4
        with:
          name: choco-ship-ios-project
          path: app-build/choco-ship-ios-project.zip
          if-no-files-found: error
